services:
  teleorderbot:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: teleorderbot
    restart: unless-stopped
    init: true

    env_file:
      - config.env

    # Local health endpoint only. Telegram updates are received with long polling.
    # No automatic QRIS/payment webhook is used.
    ports:
      - "127.0.0.1:${APP_PORT:-8080}:8080"

    volumes:
      - ./data:/app/data
      - ./logs:/app/logs

    environment:
      HOST: "0.0.0.0"
      PORT: "8080"

    security_opt:
      - no-new-privileges:true

    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8080/health', timeout=3).read()"
        ]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s
